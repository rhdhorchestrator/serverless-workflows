{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "switch": {
      "title": "switch",
      "description": "Type of output for the workflow",
      "type": "string",
      "enum": ["OK", "KO", "Wait"],
      "default": "OK"
    }
  }
}
